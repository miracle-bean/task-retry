server:
  port: 8080
spring:
  application:
    name: task-retry
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/db_name?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: com.task.retry.mapper/*.xml
  type-aliases-package: com.task.retry.mapper